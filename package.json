{
  "name": "opencode-plugin-template",
  "version": "0.1.0",
  "author": "Vito Lin",
  "license": "MIT",
  "main": "dist/index.js",
  "description": "Template for building OpenCode plugins with TypeScript",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "bun test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky install"
  },
  "opencode": {
    "plugin": true
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,md}": [
      "bunx prettier --write"
    ]
  },
  "type": "module",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@opencode-ai/plugin": "^1.1.36"
  },
  "devDependencies": {
    "bun-types": "^1.3.6",
    "coverage-badges-cli": "^2.2.0",
    "husky": "^8.0.3",
    "lcov-badge2": "^1.1.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.5",
    "typescript": "^5.9.3"
  }
}
